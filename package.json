{
  "name": "vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "json-server db.json --port 3000",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
